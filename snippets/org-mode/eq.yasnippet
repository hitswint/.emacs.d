# -*- mode: snippet -*-
# contributor: Mads D. Kristensen <madsdk@gmail.com>
# key: eq
# name: \begin{equation} ... \end{equation}
# --
${1:"choose from export format..."$(unless yas-modified-p (let ((export-to (ivy-read "Export to: " '(office latex)))) (if (string-equal export-to "office") "" "# ")))}#+NAME: ${2:$$(unless yas-modified-p (reftex-label "equation" 'dont-insert))}
\begin{equation}
${3:$$(unless yas-modified-p (if (string-equal (yas-field-value 1) "# ") (format "\\label\{%s\}\n" (yas-field-value 2))))}$0
\end{equation}